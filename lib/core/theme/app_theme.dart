import 'package:flutter/material.dart';

/// Placeholder dark theme.
///
/// Superseded by the design-token theme layer (E1.1), which will expose
/// accent, semantic, and typography tokens from the design system.
ThemeData buildAppTheme() {
  return ThemeData.dark(useMaterial3: true);
}
