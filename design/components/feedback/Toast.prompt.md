# Toast

Transient confirmation pill; every destructive swipe (Done, snooze) gets one with Undo. Position it above the tab bar.

```jsx
<Toast action="Undo" onAction={fn}>To-do marked as done</Toast>
```
