# CI icon

GitLab's circular pipeline-status badge icon; the status→color mapping (passed green, running blue, failed red, pending orange, the rest neutral) is the brand's identity — never reassign it.

```jsx
<CiIcon status="running" size={20} />
```

Statuses: success, failed, running, pending, warning, canceled, skipped, manual, created, scheduled.
