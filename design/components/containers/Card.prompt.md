# Card

Grouping container (12px radius, hairline border) — holds tile lists, merge boxes, settings groups. Fill with ListRows (padded=false) or free content (padded).

```jsx
<Card><ListRow … /><ListRow … /></Card>
```
