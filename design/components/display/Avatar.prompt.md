Avatar for a user (circle) or project/group (rounded square); tinted-initials fallback uses Pajamas' six avatar hues.

```jsx
<Avatar name="Marin Katsuragi" size={32} />
<Avatar name="gitsune" entity="project" size={48} src="…" />
```
