GitLab issue/MR label pill; `scope::value` names render the signature two-tone scoped pill automatically.

```jsx
<Label name="bug" color="#dd2b0e" />
<Label name="workflow::in review" color="#7b58cf" />
```
