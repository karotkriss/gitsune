# Skeleton

Shimmering placeholder bar(s) shown while content loads — the offline-first app's constant companion.

```jsx
<Skeleton lines={3} />
<Skeleton width={120} height={20} radius="var(--gl-border-radius-full)" />
```
